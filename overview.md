# 电商商单智能体插件 — 交付总览

> 交付时间：2026-08-25 ｜ 目标平台：DeepSeek Harness (dsh) v0.1.1-rc.2

## 一、交付内容

针对需求「优化电商商单操作，商品管理与订单处理」，基于视频《DeepSeek Harness 电商店铺数据分析智能体》的功能定位（+ 用户确认），交付一个完整的 dsh 插件：

**ecommerce-analyst-plugin**（`E:\dsh\deepseek-harness-master\deepseek-harness-master\ecommerce-analyst-plugin\`）

| 交付物 | 路径 | 说明 |
|---|---|---|
| 功能规范文档 | `docs/functional-spec.md` | 视频分析、可行性、技术挑战、功能规范、API 对接、测试验收 |
| 插件源码 | `src/`（多 TS 模块） | 订单/分析/库存工具集 + 数据层 + 适配器 + 报表解析 + 数据中台 |
| 预置示例数据 | `data/seed.json` | 26 商品 + 480 订单（含逾期/低库存/退款异常） |
| 测试套件 | `tests/` | **106 项测试全部通过** |
| 使用说明 | `README.md` | 加载方式、对话示例、平台对接、开发测试 |

## 二、功能清单（订单/分析/库存 + 报表导入 + 备份）

1. **商品查询**：查询/筛选（分类、状态、价格、关键词）；商品数据完全由「导入」或「平台 API」决定，无手动增删改查
2. **订单处理**：查询（状态、日期、金额、买家）/ 统计 / 状态流转（合法性校验）/ 发货 / 退款
3. **销售数据分析**：经营总览 / 日周月趋势 / TOP 商品排行 / 类目分布（统一统计口径）
4. **库存预警**：低库存清单（阈值可配）/ 补货建议（近 30 天销量 × 1.5 安全系数）
5. **月度/周度复盘导入 + 数据中台**：月度 4 份 Excel（三份「商品排名导出」+「利润表」）/ 周度 3 份 Excel 解析为经营分析面板，周期严格隔离
6. **数据备份**：JSON 导出 / 导入恢复（恢复前自动快照）

## 三、质量验证结果

| 验证项 | 结果 |
|---|---|
| 单元测试 | ✅ 106/106 通过（状态机、统计口径手工核算、阈值边界、持久化、工具注册、周期隔离、导出接口、入口冒烟、UI 完整性） |
| 端到端测试 | ✅ 53 项断言通过（生成物理 Excel 测试文件 → 导入/导出/分析 → 自动清理） |
| 运行环境 | ✅ Node ≥ 22.19，dsh workspace 依赖解析正常 |

## 四、数据接入方案（已确认：对接电商平台 API）

- **默认示例模式（mock）**：无凭证开箱即用，数据只读演示
- **REST 模式（rest）**：配置 `ecommerceAnalyst.platform.*` 对接平台开放平台；`signParams()` 为平台签名钩子，按淘宝/拼多多/抖店文档实现即可接入
- **安全**：凭证走环境变量 `DSH_ECOM_TOKEN`，不硬编码；API 失败自动降级示例模式

## 五、待迭代项（后续版本）

| 版本 | 内容 |
|---|---|
| v0.2.0 | 对话流业务卡片 UI（分类/筛选交互，对应「侧边栏展开交互」） |
| v0.3.0 | 接入第一个真实电商平台适配器（需用户提供平台与凭证） |
| v0.4.0 | 侧边栏插槽（随 dsh 官方文档演进） |

## 六、已知说明

- 微信视频号视频流无法被程序解析，功能清单基于标题/标签/公开资料推断并经用户确认（详见功能规范 §1.1）
- 插件的 workspace 依赖（`@deepseek-ai/cordis` 等）通过 junction 链接到仓库 vendor/packages（pnpm install 在本机 safe-delete 失败，已用手动链接解决）
